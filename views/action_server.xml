<odoo>
    <record id="product_template" model="ir.actions.server">
        <field name="name">Mettre à jours Prix UOM</field>
        <field name="model_id" ref="product.model_product_template"/>
        <field name="state">code</field>
        <field name="code">
            action = records.update_multi_uom_price()
        </field>
    </record>

</odoo>